{% trans_default_domain 'FOSUserBundle' %}

{% extends "base_basic.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">Registrar-se</h3>
                    </div>
                    <div class="panel-body">
                        {% for msg in app.session.flashBag.get('success') %}
                        {% endfor %}
                        <p>Parabéns, {{ user.username }}. A sua conta foi criada e ativada.</p>
                        <br/>
                        <p><a href="{{ path('home') }}" class="btn btn-lg btn-success btn-block">Ir para a home page</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
