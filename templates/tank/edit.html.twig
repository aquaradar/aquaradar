{% extends 'base.html.twig' %}

{% block title %}Editar Reservatório - {{ parent() }}{% endblock %}

{% block body %}
    <!-- Page Content -->
    <div id="page-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Editar Reservatório</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->

            <div class="row">
                <div class="col-xs-12">
                    {{ include('tank/_form.html.twig') }}

                    <br/>
                    <a href="{{ path('tank_index') }}">Voltar para lista de reservatórios</a>
                    {{ include('tank/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
        <!-- /.container-fluid -->
    </div>
    <!-- /#page-wrapper -->
{% endblock %}
