{% extends 'base.html.twig' %}

{% block title %}Tank{% endblock %}

{% block body %}
    <h1>Tank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tank.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tank.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ tank.address }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ tank.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ tank.longitude }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ tank.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tank_index') }}">back to list</a>

    <a href="{{ path('tank_edit', {'id': tank.id}) }}">edit</a>

    {{ include('tank/_delete_form.html.twig') }}
{% endblock %}
